#include<stdio.h>
int main()
    {
        int a,b,;
        printf("Enter the two numbers:");
        scanf("%d%d",&a,&b);
        sum=a+b;
        printf("Addition of two numbers=%d",sum);
        retuen 0
    }
)